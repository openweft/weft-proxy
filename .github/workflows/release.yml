name: release

# Builds weft-proxy for the OS×arch matrix weft-agent ships on, publishes
# the binaries as a GitHub Release, and pushes a multi-arch OCI image to
# ghcr.io so `weft up` can pull weft-proxy alongside the kernel + drivers.
#
# Trigger policy mirrors the rest of openweft: workflow_dispatch + tag
# `v*` only, never push:main (per feedback_no_autopublish_dev).
#
# Supply-chain attestations: each tarball + per-arch image gets an
# SPDX 2.3 JSON SBOM (Syft) cosign-attested as `spdxjson`, plus a
# SLSA L3 provenance via slsa-framework/slsa-github-generator. See
# the weft repo's docs/operations/cosign-verify.md for the verifier
# commands ; the workflow path differs (this repo) but the identity
# pinning pattern is identical.

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Override tag (default: refs/tags/<current>)"
        required: false
  push:
    tags: ['v*']

permissions:
  contents: write
  packages: write

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/weft-proxy

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: build
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          set -euo pipefail
          out="dist/${{ matrix.goos }}-${{ matrix.goarch }}/weft-proxy"
          mkdir -p "$(dirname "$out")"
          go build -trimpath -ldflags "-s -w" -o "$out" .

      - uses: actions/upload-artifact@v4
        with:
          name: weft-proxy-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/${{ matrix.goos }}-${{ matrix.goarch }}/weft-proxy

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    outputs:
      hashes: ${{ steps.hashes.outputs.hashes }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: false

      - name: stage release assets
        # GitHub Release expects flat files. Repackage each per-OS/arch
        # binary as a tar.gz alongside a SHA256SUMS file.
        run: |
          set -euo pipefail
          mkdir -p stage
          tag="${{ github.ref_name }}"
          for d in dist/weft-proxy-*; do
            name=$(basename "$d")
            tar -C "$d" -czf "stage/${name}_${tag}.tar.gz" weft-proxy
          done
          ( cd stage && sha256sum *.tar.gz > SHA256SUMS )

      - uses: anchore/sbom-action/download-syft@v0

      - name: generate SBOMs (SPDX 2.3 JSON, per tarball)
        # One SBOM per per-OS/arch tarball. We scan the per-arch
        # build directory so the SBOM enumerates the Go modules
        # baked into that arch.
        run: |
          set -euo pipefail
          for d in dist/weft-proxy-*; do
            name=$(basename "$d")
            syft scan "dir:${d}" -o spdx-json="stage/${name}_${{ github.ref_name }}.tar.gz.spdx.json"
          done

      - name: compute SHA256SUMS hash for SLSA
        # base64(sha256sum *.tar.gz) feeds the SLSA generic generator
        # below ; one provenance attestation covers every tarball.
        id: hashes
        run: |
          set -euo pipefail
          cd stage
          echo "hashes=$(sha256sum *.tar.gz | base64 -w0)" >>"$GITHUB_OUTPUT"

      - uses: sigstore/cosign-installer@v3

      - name: sign release artifacts (keyless)
        # Sigstore keyless: OIDC identity = GitHub Actions runner. Drops
        # a .sig + .cert next to each tarball so consumers can verify
        # with `cosign verify-blob --certificate <name>.cert --signature
        # <name>.sig --certificate-identity-regexp '...' --certificate-
        # oidc-issuer https://token.actions.githubusercontent.com`.
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: |
          set -euo pipefail
          cd stage
          for f in *.tar.gz; do
            cosign sign-blob --yes \
              --output-signature "${f}.sig" \
              --output-certificate "${f}.cert" \
              "$f"
          done

      - name: cosign-attest SBOMs (spdxjson predicate)
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: |
          set -euo pipefail
          cd stage
          for f in *.tar.gz; do
            cosign attest-blob --yes \
              --predicate "${f}.spdx.json" \
              --type spdxjson \
              --output-attestation "${f}.spdx.intoto.jsonl" \
              "$f"
          done

      - name: create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            stage/*.tar.gz
            stage/*.tar.gz.sig
            stage/*.tar.gz.cert
            stage/*.spdx.json
            stage/*.spdx.intoto.jsonl
            stage/SHA256SUMS
          draft: true
          generate_release_notes: true

  provenance-tarballs:
    # SLSA L3 provenance for tarballs. The reusable workflow runs on
    # an isolated ephemeral builder GitHub controls — that's what
    # earns L3 ("builder identity non-falsifiable"). Drops a
    # .intoto.jsonl alongside the existing draft release assets.
    needs: release
    permissions:
      contents: write
      id-token: write
      actions: read
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.0.0
    with:
      base64-subjects: ${{ needs.release.outputs.hashes }}
      upload-assets: true
      draft-release: true

  image:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
    outputs:
      image-amd64: ${{ steps.expose.outputs.image-amd64 }}
      image-arm64: ${{ steps.expose.outputs.image-arm64 }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: weft-proxy-linux-${{ matrix.arch }}
          path: image/

      - name: chmod
        run: chmod +x image/weft-proxy

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: write Dockerfile
        run: |
          cat > image/Dockerfile <<'EOF'
          FROM gcr.io/distroless/static-debian12:nonroot
          COPY weft-proxy /usr/local/bin/weft-proxy
          ENTRYPOINT ["/usr/local/bin/weft-proxy"]
          CMD ["run", "--config", "-"]
          EOF

      - name: build + push per-arch image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: image
          platforms: linux/${{ matrix.arch }}
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ github.ref_name }}-${{ matrix.arch }}
            ${{ env.IMAGE }}:latest-${{ matrix.arch }}

      - uses: sigstore/cosign-installer@v3

      - name: sign per-arch image (keyless)
        # Sign the per-arch manifest by its content-addressed digest so
        # the signature stays bound even if a tag is later moved.
        env:
          COSIGN_EXPERIMENTAL: "1"
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          set -euo pipefail
          cosign sign --yes "${IMAGE}@${DIGEST}"

      - uses: anchore/sbom-action/download-syft@v0

      - name: generate per-arch image SBOM (SPDX 2.3 JSON)
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          set -euo pipefail
          syft scan "registry:${IMAGE}@${DIGEST}" \
            -o spdx-json=weft-proxy-image-${{ matrix.arch }}.spdx.json

      - name: cosign-attest image SBOM (spdxjson predicate)
        env:
          COSIGN_EXPERIMENTAL: "1"
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          set -euo pipefail
          cosign attest --yes \
            --predicate "weft-proxy-image-${{ matrix.arch }}.spdx.json" \
            --type spdxjson \
            "${IMAGE}@${DIGEST}"

      - name: upload image SBOM to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: weft-proxy-image-${{ matrix.arch }}.spdx.json
          draft: true

      - name: expose digest for SLSA
        id: expose
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          echo "image-${{ matrix.arch }}=${DIGEST#sha256:}" >>"$GITHUB_OUTPUT"

  provenance-image-amd64:
    needs: image
    permissions:
      contents: read
      packages: write
      id-token: write
      actions: read
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.0.0
    with:
      image: ghcr.io/${{ github.repository_owner }}/weft-proxy
      digest: sha256:${{ needs.image.outputs.image-amd64 }}
      registry-username: ${{ github.actor }}
    secrets:
      registry-password: ${{ secrets.GITHUB_TOKEN }}

  provenance-image-arm64:
    needs: image
    permissions:
      contents: read
      packages: write
      id-token: write
      actions: read
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.0.0
    with:
      image: ghcr.io/${{ github.repository_owner }}/weft-proxy
      digest: sha256:${{ needs.image.outputs.image-arm64 }}
      registry-username: ${{ github.actor }}
    secrets:
      registry-password: ${{ secrets.GITHUB_TOKEN }}

  manifest:
    needs: image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: assemble multi-arch manifest
        id: manifest
        run: |
          set -euo pipefail
          for tag in "${{ github.ref_name }}" latest; do
            docker buildx imagetools create \
              --tag "${{ env.IMAGE }}:${tag}" \
              "${{ env.IMAGE }}:${tag}-amd64" \
              "${{ env.IMAGE }}:${tag}-arm64"
          done
          # Resolve the multi-arch index digest for the primary tag so we
          # can pin the cosign signature to the content, not the tag.
          digest=$(docker buildx imagetools inspect \
            "${{ env.IMAGE }}:${{ github.ref_name }}" \
            --format '{{.Manifest.Digest}}')
          echo "digest=${digest}" >>"$GITHUB_OUTPUT"

      - uses: sigstore/cosign-installer@v3

      - name: sign multi-arch manifest (keyless)
        env:
          COSIGN_EXPERIMENTAL: "1"
          DIGEST: ${{ steps.manifest.outputs.digest }}
        run: |
          set -euo pipefail
          cosign sign --yes "${IMAGE}@${DIGEST}"
